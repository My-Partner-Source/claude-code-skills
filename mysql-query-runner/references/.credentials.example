# MySQL Database Credentials
# ==========================
#
# Each environment connects to a different host.
# Copy this file to .credentials and fill in your actual values.
#
# SECURITY WARNING:
# - Never commit .credentials to version control
# - File permissions should be 600 (owner read/write only)
# - Rotate passwords regularly
#
# Usage:
#   cp .credentials.example .credentials
#   # Edit .credentials with your values
#   # Or run: /credential-setup mysql-query-runner
#
# MULTI-DATABASE ACCESS:
#   DATABASE is optional. Leave empty to access multiple databases.
#   When empty, use fully qualified table names in queries:
#     SELECT * FROM intfstagedb.tablename
#     SELECT * FROM metadataconfdb.othertable

# =============================================================================
# DEV Environment
# =============================================================================
export MYSQL_DEV_HOST="dev-db.internal.yourcompany.com"
export MYSQL_DEV_PORT="3306"
export MYSQL_DEV_USER="your-username"
export MYSQL_DEV_PASSWORD="your-password"
# DATABASE is optional - leave empty for multi-database access
export MYSQL_DEV_DATABASE=""

# =============================================================================
# QA Environment
# =============================================================================
export MYSQL_QA_HOST="qa-db.internal.yourcompany.com"
export MYSQL_QA_PORT="3306"
export MYSQL_QA_USER="your-username"
export MYSQL_QA_PASSWORD="your-password"
# DATABASE is optional - leave empty for multi-database access
export MYSQL_QA_DATABASE=""

# =============================================================================
# UAT Environment
# =============================================================================
export MYSQL_UAT_HOST="uat-db.internal.yourcompany.com"
export MYSQL_UAT_PORT="3306"
export MYSQL_UAT_USER="your-username"
export MYSQL_UAT_PASSWORD="your-password"
# DATABASE is optional - leave empty for multi-database access
export MYSQL_UAT_DATABASE=""

# =============================================================================
# PROD Environment (CAUTION: Production database)
# =============================================================================
# WARNING: Be extremely careful with PROD credentials
# Consider using a read-only user for safety
export MYSQL_PROD_HOST="prod-db.internal.yourcompany.com"
export MYSQL_PROD_PORT="3306"
export MYSQL_PROD_USER="your-username"
export MYSQL_PROD_PASSWORD="your-password"
# DATABASE is optional - leave empty for multi-database access
export MYSQL_PROD_DATABASE=""
