# Bitbucket Credentials Template
#
# SECURITY: This is a template file. Copy it to .credentials and fill in your actual values.
# DO NOT commit .credentials to version control - it's in .gitignore
#
# Setup:
#   1. Copy this file: cp .credentials.example .credentials
#   2. Edit .credentials with your actual credentials
#   3. Source before using: source bitbucket-repo-lookup/references/.credentials
#   4. Run scripts: python bitbucket-repo-lookup/scripts/bitbucket_api.py list

# ============================================================================
# Option 1: App Password (Recommended)
# ============================================================================
#
# App passwords are more secure than account passwords and can be revoked independently.
#
# To create an app password:
#   1. Go to Bitbucket → Personal settings → App passwords
#   2. Click "Create app password"
#   3. Label: "claude-code-skill" (or your preference)
#   4. Permissions: Select "Repositories: Read" (required)
#   5. Copy the generated password (you won't see it again)
#
# Uncomment and fill in:
export BITBUCKET_USERNAME="your-username-here"
export BITBUCKET_APP_PASSWORD="your-app-password-here"

# ============================================================================
# Option 2: Access Token (For workspace/CI-CD access)
# ============================================================================
#
# Use this for workspace-level access or CI/CD pipelines.
#
# Uncomment and fill in (comment out Option 1 if using this):
# export BITBUCKET_ACCESS_TOKEN="your-access-token-here"

# ============================================================================
# Bitbucket Server / Data Center Configuration (Optional)
# ============================================================================
#
# If you're using a self-hosted Bitbucket Server or Data Center instance
# (instead of Bitbucket Cloud), uncomment and set the base URL:
#
# export BITBUCKET_BASE_URL="https://your-bitbucket-server.example.com/rest/api/1.0"
#
# For Bitbucket Cloud, this is not needed (uses default: api.bitbucket.org/2.0)

# ============================================================================
# Verification
# ============================================================================
#
# After sourcing this file, verify credentials are set:
#   echo $BITBUCKET_USERNAME
#   echo $BITBUCKET_APP_PASSWORD  # Should show your password
#
# Test the connection:
#   python bitbucket-repo-lookup/scripts/bitbucket_api.py list --workspace your-workspace-slug
