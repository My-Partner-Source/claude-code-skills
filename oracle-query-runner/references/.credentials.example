# Oracle Database Credentials
# ===========================
#
# Each environment connects to a different host/service.
# Copy this file to .credentials and fill in your actual values.
#
# SECURITY WARNING:
# - Never commit .credentials to version control
# - File permissions should be 600 (owner read/write only)
# - Rotate passwords regularly
#
# Usage:
#   cp .credentials.example .credentials
#   # Edit .credentials with your values
#   # Or run: /credential-setup oracle-query-runner
#
# CONNECTION FORMAT:
#   Uses Easy Connect format: host:port/service_name
#   Service name (not SID) is recommended for modern Oracle databases.

# =============================================================================
# DEV Environment
# =============================================================================
export ORACLE_DEV_HOST="dev-oracle.internal.yourcompany.com"
export ORACLE_DEV_PORT="1521"
export ORACLE_DEV_USER="your-username"
export ORACLE_DEV_PASSWORD="your-password"
export ORACLE_DEV_SERVICE="devdb.yourcompany.com"

# =============================================================================
# QA Environment
# =============================================================================
export ORACLE_QA_HOST="qa-oracle.internal.yourcompany.com"
export ORACLE_QA_PORT="1521"
export ORACLE_QA_USER="your-username"
export ORACLE_QA_PASSWORD="your-password"
export ORACLE_QA_SERVICE="qadb.yourcompany.com"

# =============================================================================
# UAT Environment
# =============================================================================
export ORACLE_UAT_HOST="uat-oracle.internal.yourcompany.com"
export ORACLE_UAT_PORT="1521"
export ORACLE_UAT_USER="your-username"
export ORACLE_UAT_PASSWORD="your-password"
export ORACLE_UAT_SERVICE="uatdb.yourcompany.com"

# =============================================================================
# PROD Environment (CAUTION: Production database)
# =============================================================================
# WARNING: Be extremely careful with PROD credentials
# Consider using a read-only user for safety
export ORACLE_PROD_HOST="prod-oracle.internal.yourcompany.com"
export ORACLE_PROD_PORT="1521"
export ORACLE_PROD_USER="your-username"
export ORACLE_PROD_PASSWORD="your-password"
export ORACLE_PROD_SERVICE="proddb.yourcompany.com"
