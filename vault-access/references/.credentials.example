# HashiCorp Vault Credentials
# ===========================
#
# Copy this file to .credentials and fill in your actual values.
#
# SECURITY WARNING:
# - Never commit .credentials to version control
# - File permissions should be 600 (owner read/write only)
# - Use tokens with short TTL when possible
# - Rotate tokens regularly
#
# Usage:
#   cp .credentials.example .credentials
#   # Edit .credentials with your values
#   # Or run: /credential-setup vault-access
#
# GETTING A VAULT TOKEN:
#   From CLI:  vault login -method=ldap username=youruser && vault token create
#   From UI:   Log in > Click user icon > Copy Token
#   Service:   Contact Vault admin for AppRole credentials

# =============================================================================
# Vault Server Configuration
# =============================================================================

# Vault server URL (required)
# Examples:
#   https://vault.yourcompany.com:8200
#   https://vault.internal:8200
#   http://localhost:8200 (dev only)
export VAULT_ADDR="https://vault.yourcompany.com:8200"

# Authentication token (required)
# Get from: vault token create, or copy from Vault UI
export VAULT_TOKEN="hvs.your-vault-token-here"

# =============================================================================
# Optional Settings
# =============================================================================

# Enterprise namespace (optional - leave empty for OSS Vault)
# Used in Vault Enterprise for multi-tenant setups
export VAULT_NAMESPACE=""

# Skip TLS certificate verification (optional)
# WARNING: Only use for development/testing, never in production
# Values: "true" or "false" (default: false)
export VAULT_SKIP_VERIFY="false"

# Default KV version (optional)
# Values: "1", "2", or "auto" (default: auto)
# Auto-detection works for most cases
export VAULT_KV_VERSION="auto"
