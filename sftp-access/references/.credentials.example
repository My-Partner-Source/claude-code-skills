# SFTP Server Credentials
# =======================
#
# Copy this file to .credentials and fill in your actual values.
#
# SECURITY WARNING:
# - Never commit .credentials to version control
# - File permissions should be 600 (owner read/write only)
# - Use SSH key authentication when possible
# - Rotate credentials regularly
#
# Usage:
#   cp .credentials.example .credentials
#   # Edit .credentials with your values
#   chmod 600 .credentials
#   # Or run: /credential-setup sftp-access

# =============================================================================
# SFTP Server Configuration (Required)
# =============================================================================

# SFTP server hostname or IP address
export SFTP_HOST="sftp.example.com"

# SFTP server port (optional, default: 22)
export SFTP_PORT="22"

# Username for authentication
export SFTP_USERNAME="your-username"

# =============================================================================
# Authentication (Choose one method)
# =============================================================================

# Option 1: Password Authentication
# ---------------------------------
# Simple but less secure than SSH keys
export SFTP_PASSWORD="your-password"

# Option 2: SSH Key Authentication (Recommended)
# ----------------------------------------------
# More secure than passwords, supports passphrase protection
#
# Generate a new SSH key:
#   ssh-keygen -t ed25519 -C "your-email@example.com"
#
# Add public key to remote server:
#   ssh-copy-id -i ~/.ssh/id_ed25519.pub user@sftp.example.com
#
# Set the private key path below:
# export SFTP_KEY_FILE="/home/your-user/.ssh/id_ed25519"

# Passphrase for encrypted private key (optional)
# Only needed if your private key is protected with a passphrase
# export SFTP_KEY_PASSPHRASE="your-key-passphrase"

# =============================================================================
# Notes
# =============================================================================
#
# Authentication Priority:
#   1. If SFTP_KEY_FILE is set, SSH key authentication is used
#   2. Otherwise, SFTP_PASSWORD is used
#
# SSH Key Types Supported:
#   - Ed25519 (recommended, most secure)
#   - RSA (widely compatible)
#   - ECDSA
#   - DSS (legacy)
#
# Troubleshooting:
#   - Test SSH connectivity: ssh -v user@host -p 22
#   - Verify key permissions: ls -la ~/.ssh/id_* (should be 600)
#   - Test key manually: ssh -i ~/.ssh/id_ed25519 user@host
