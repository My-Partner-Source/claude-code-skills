# AWS S3 Credentials
# ==================
#
# Copy this file to .credentials and fill in your actual values.
#
# SECURITY WARNING:
# - Never commit .credentials to version control
# - File permissions should be 600 (owner read/write only)
# - Rotate access keys regularly
# - Use IAM policies with minimum required permissions
#
# Usage:
#   cp .credentials.example .credentials
#   # Edit .credentials with your values
#   # Or run: /credential-setup s3-access
#
# GETTING AWS CREDENTIALS:
#   From Console: IAM > Users > Your User > Security credentials > Create access key
#   From CLI:     aws configure (creates ~/.aws/credentials)
#   Using SSO:    aws sso login --profile your-profile

# =============================================================================
# AWS Credentials Configuration
# =============================================================================

# AWS Access Key ID (required unless using profile)
# Get from: AWS Console > IAM > Users > Security credentials
export AWS_ACCESS_KEY_ID="AKIAIOSFODNN7EXAMPLE"

# AWS Secret Access Key (required unless using profile)
# Get from: AWS Console > IAM > Users > Security credentials
export AWS_SECRET_ACCESS_KEY="wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"

# =============================================================================
# Optional Settings
# =============================================================================

# Default AWS Region (optional)
# Common regions: us-east-1, us-west-2, eu-west-1, ap-southeast-1
# Default: us-east-1
export AWS_DEFAULT_REGION="us-east-1"

# AWS Profile (optional)
# Use a named profile from ~/.aws/credentials instead of access keys
# If set, AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY are ignored
# export AWS_PROFILE="default"

# Custom Endpoint URL (optional)
# Use for VPC endpoints, LocalStack, MinIO, or other S3-compatible services
# Examples:
#   https://s3.us-east-1.vpce.amazonaws.com  (VPC endpoint)
#   http://localhost:4566                     (LocalStack)
#   http://minio:9000                         (MinIO)
# export AWS_ENDPOINT_URL=""

# Session Token (optional)
# Required when using temporary credentials (STS)
# export AWS_SESSION_TOKEN=""
