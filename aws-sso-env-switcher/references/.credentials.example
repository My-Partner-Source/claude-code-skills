# AWS SSO Environment Switcher Credentials
# =========================================
#
# Copy this file to .credentials and fill in your actual values.
#
# SECURITY WARNING:
# - Never commit .credentials to version control
# - File permissions should be 600 (owner read/write only)
# - These values configure SSO profiles, not static credentials
#
# Usage:
#   cp .credentials.example .credentials
#   # Edit .credentials with your values
#   chmod 600 .credentials
#
# PREREQUISITES:
#   - AWS CLI v2 installed (for SSO support)
#   - kubectl installed (for EKS cluster management)
#   - Access to AWS SSO portal

# =============================================================================
# AWS SSO Configuration (Required)
# =============================================================================

# SSO Portal URL
# Find this in your AWS SSO settings or ask your administrator
# Example: https://mycompany.awsapps.com/start
export AWS_SSO_START_URL="https://your-company.awsapps.com/start"

# SSO Region
# The AWS region where your SSO is configured
export AWS_SSO_REGION="us-east-1"

# =============================================================================
# DEV Environment
# =============================================================================

# AWS Account ID for DEV
export AWS_DEV_SSO_ACCOUNT_ID="123456789012"

# SSO Role name to assume in DEV
# This role must exist in your SSO permission sets
export AWS_DEV_SSO_ROLE_NAME="DeveloperAccess"

# EKS Cluster name in DEV (optional, leave empty if no EKS)
export AWS_DEV_EKS_CLUSTER="my-app-dev-eks"

# EKS Cluster region (optional, defaults to AWS_SSO_REGION)
export AWS_DEV_EKS_REGION="us-east-1"

# Default namespace for kubectl commands (optional)
export AWS_DEV_NAMESPACE="default"

# =============================================================================
# QA Environment
# =============================================================================

# AWS Account ID for QA
export AWS_QA_SSO_ACCOUNT_ID="234567890123"

# SSO Role name to assume in QA
export AWS_QA_SSO_ROLE_NAME="DeveloperAccess"

# EKS Cluster name in QA
export AWS_QA_EKS_CLUSTER="my-app-qa-eks"

# EKS Cluster region (optional)
export AWS_QA_EKS_REGION="us-east-1"

# Default namespace for kubectl commands (optional)
export AWS_QA_NAMESPACE="default"

# =============================================================================
# UAT Environment
# =============================================================================

# AWS Account ID for UAT
export AWS_UAT_SSO_ACCOUNT_ID="345678901234"

# SSO Role name to assume in UAT
export AWS_UAT_SSO_ROLE_NAME="DeveloperAccess"

# EKS Cluster name in UAT
export AWS_UAT_EKS_CLUSTER="my-app-uat-eks"

# EKS Cluster region (optional)
export AWS_UAT_EKS_REGION="us-east-1"

# Default namespace for kubectl commands (optional)
export AWS_UAT_NAMESPACE="default"

# =============================================================================
# PROD Environment
# =============================================================================

# AWS Account ID for PROD
export AWS_PROD_SSO_ACCOUNT_ID="456789012345"

# SSO Role name to assume in PROD
# Consider using a read-only role for production
export AWS_PROD_SSO_ROLE_NAME="ReadOnlyAccess"

# EKS Cluster name in PROD
export AWS_PROD_EKS_CLUSTER="my-app-prod-eks"

# EKS Cluster region (optional)
export AWS_PROD_EKS_REGION="us-east-1"

# Default namespace for kubectl commands (optional)
export AWS_PROD_NAMESPACE="production"

# =============================================================================
# NOTES
# =============================================================================
#
# Finding Your SSO Information:
#   1. Log into your AWS SSO portal
#   2. Click on "AWS Account" to see available accounts
#   3. Expand an account to see available roles
#   4. The "Access keys" link shows the account ID
#
# Finding Your EKS Cluster:
#   1. Go to AWS Console > EKS
#   2. Select the cluster to see its name
#   3. Or run: aws eks list-clusters --profile <profile>
#
# SSO Role Names:
#   Common role names include:
#   - AdministratorAccess
#   - DeveloperAccess
#   - ReadOnlyAccess
#   - PowerUserAccess
#   Ask your AWS administrator for available roles.
#
# Multi-Region Setup:
#   If your clusters are in different regions, set AWS_{ENV}_EKS_REGION
#   for each environment. Otherwise, it defaults to AWS_SSO_REGION.
